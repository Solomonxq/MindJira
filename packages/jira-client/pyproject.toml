
[tool.poetry]
name = "jira-client"
version = "0.1.0"
description = "Shared Jira REST API v3 client"
[tool.poetry.dependencies]
python = "^3.12"
httpx = "^0.27.0"
pydantic = "^2.7.4"
pydantic-settings = "^2.3.3"
redis = "^5.0.0"
tenacity = "^8.3.0"
structlog = "^24.2.0"
[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
 
